import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'postulate_erp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
